# Инструкция по Запуску Проекта

## Быстрый Старт (для нетерпеливых 🚀)

```bash
# 1. Клонируй репозиторий
git clone https://github.com/your-username/ab-test-marketplace-ads.git
cd ab-test-marketplace-ads

# 2. Установи зависимости
pip install -r requirements.txt

# 3. Объедини CSV файлы
python scripts/combine_csv_files.py

# 4. Открой ноутбуки в Jupyter и выполни по порядку
jupyter notebook
```

---

## Подробная Инструкция

### Шаг 1: Клонирование Репозитория

Если ты работаешь локально на компьютере:

```bash
git clone https://github.com/your-username/ab-test-marketplace-ads.git
cd ab-test-marketplace-ads
```

Если ты работаешь в Google Colab:

```python
!git clone https://github.com/your-username/ab-test-marketplace-ads.git
%cd ab-test-marketplace-ads
```

---

### Шаг 2: Подготовка Окружения

#### Вариант A: Локально на компьютере

```bash
# Создай виртуальное окружение (опционально, но рекомендуется)
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установи зависимости
pip install -r requirements.txt
```

#### Вариант B: В Google Colab

```python
!pip install -r requirements.txt
```

---

### Шаг 3: Подготовка Данных

#### Если у тебя есть исходные CSV файлы (data_raw_part1.csv, data_raw_part2.csv и т.д.)

```bash
# Используй скрипт для объединения
python scripts/combine_csv_files.py
```

Скрипт автоматически:
- Найдет все файлы `data_raw_part*.csv` в папке `data/raw/`
- Объединит их в один файл `data_raw_combined.csv`
- Сохранит результат в `data/processed/`

#### Если твои данные уже подготовлены

Убедись, что в папке `data/processed/` есть файл:
- `final_results_to_analyze.xlsx` — финальный датасет для анализа

---

### Шаг 4: Запуск Ноутбуков

#### Вариант A: Локально в Jupyter Notebook

```bash
# Запусти Jupyter
jupyter notebook

# В браузере откроется интерфейс Jupyter
# Перейди в папку notebooks/ и открывай ноутбуки по порядку
```

#### Вариант B: В Google Colab

```python
# Для каждого ноутбука
from IPython.display import IFrame
IFrame(src='https://colab.research.google.com/github/your-username/ab-test-marketplace-ads/blob/main/notebooks/01_Data_Preparation.ipynb', width=800, height=600)
```

Или просто открой каждый ноутбук через:
`https://colab.research.google.com/github/your-username/ab-test-marketplace-ads/blob/main/notebooks/XXX.ipynb`

---

### Шаг 5: Порядок Выполнения Ноутбуков

Выполняй ноутбуки в этом порядке:

#### 1. **01_Data_Preparation.ipynb**
   - Загружает и объединяет данные
   - Выполняет проверку качества
   - Подготавливает данные для анализа
   - ⏱️ Время выполнения: 5-10 минут
   - 📊 Выход: `data/processed/final_results_to_analyze.xlsx`

#### 2. **02_MDE_Calculation.ipynb**
   - Рассчитывает минимально обнаруживаемый эффект (MDE)
   - Определяет требуемый размер выборки
   - Проверяет достаточность текущих данных
   - ⏱️ Время выполнения: 2-3 минуты
   - 📊 Выход: `reports/mde_calculation_summary.csv`

#### 3. **03_AB_Test_Analysis.ipynb**
   - Проводит статистические тесты (Chi-square, t-тест)
   - Сравнивает метрики между группами
   - Рассчитывает p-values и доверительные интервалы
   - ⏱️ Время выполнения: 3-5 минут
   - 📊 Выход: `reports/ab_test_statistical_results.csv`

#### 4. **04_Daily_Dynamics.ipynb**
   - Анализирует дневную и почасовую динамику
   - Строит графики и визуализации
   - Проверяет стабильность эффекта
   - ⏱️ Время выполнение: 5-10 минут
   - 📊 Выход: графики в `reports/visualizations/`

---

## Возможные Проблемы и Решения

### Проблема 1: "ModuleNotFoundError: No module named 'pandas'"

**Решение:**
```bash
pip install pandas numpy scipy statsmodels matplotlib seaborn
```

### Проблема 2: "FileNotFoundError: data/processed/final_results_to_analyze.xlsx"

**Решение:**
- Убедись, что файл существует в папке `data/processed/`
- Если нет — запусти ноутбук `01_Data_Preparation.ipynb` или скрипт `combine_csv_files.py`

### Проблема 3: "Memory Error при работе с большими файлами"

**Решение:**
```python
# В начале ноутбука используй:
import pandas as pd
df = pd.read_csv('file.csv', chunksize=10000)  # Читай по частям
```

### Проблема 4: "Графики не отображаются"

**Решение:**
```python
# Добавь в начало ноутбука:
%matplotlib inline
import matplotlib.pyplot as plt
```

---

## Структура Файлов в GitHub Desktop

После выполнения всех ноутбуков у тебя должна быть такая структура:

```
ab-test-marketplace-ads/
├── README.md ✅
├── README_Final.md ✅
├── requirements.txt ✅
├── .gitignore ✅
│
├── data/
│   ├── raw/
│   │   ├── data_raw_part1.csv
│   │   ├── data_raw_part2.csv
│   │   └── ... (до 20)
│   └── processed/
│       ├── data_raw_combined.csv ✅
│       └── final_results_to_analyze.xlsx ✅
│
├── notebooks/
│   ├── 01_Data_Preparation.ipynb ✅
│   ├── 02_MDE_Calculation.ipynb ✅
│   ├── 03_AB_Test_Analysis.ipynb ✅
│   └── 04_Daily_Dynamics.ipynb ✅
│
├── scripts/
│   └── combine_csv_files.py ✅
│
├── documentation/
│   ├── METHODOLOGY.md ✅
│   ├── STATISTICAL_TESTS.md ✅
│   ├── GLOSSARY.md ✅
│   └── HOW_TO_RUN.md ✅ (этот файл)
│
└── reports/
    ├── Test_Report_Final.md ✅
    ├── mde_calculation_summary.csv ✅
    ├── ab_test_statistical_results.csv ✅
    ├── ab_test_summary_table.csv ✅
    └── visualizations/
        ├── 01_daily_dynamics.png ✅
        ├── 02_hourly_dynamics.png ✅
        ├── 03_weekly_pattern.png ✅
        └── 04_stability_analysis.png ✅
```

---

## Загрузка в GitHub Desktop

После того как все файлы готовы:

```bash
# 1. Проверь статус
git status

# 2. Добавь все файлы
git add .

# 3. Создай commit
git commit -m "Add complete A/B test analysis: notebooks, reports, visualizations"

# 4. Загрузи на GitHub
git push origin main
```

---

## Полезные Команды для Разработки

```bash
# Просмотреть историю коммитов
git log --oneline

# Просмотреть различия между версиями
git diff

# Создать новую ветку для экспериментов
git checkout -b feature/new-analysis

# Вернуться на main
git checkout main
```

---

## Как Использовать Результаты

После выполнения всех ноутбуков у тебя будут:

### 📊 Данные для презентации
- `Test_Report_Final.md` — подробный отчет
- `reports/ab_test_summary_table.csv` — таблица результатов
- `reports/visualizations/*.png` — графики

### 📈 Для дальнейшего анализа
- `reports/ab_test_statistical_results.csv` — все статистические тесты
- `reports/mde_calculation_summary.csv` — параметры теста

### 🐍 Для воспроизводимости
- `notebooks/*.ipynb` — весь код анализа
- `scripts/*.py` — автоматизированные скрипты

---

## Часто Задаваемые Вопросы

**Q: Сколько времени займет выполнение всех ноутбуков?**  
A: ~20-30 минут в зависимости от производительности компьютера.

**Q: Могу ли я модифицировать ноутбуки для своих данных?**  
A: Да! Это образовательный проект, и ты можешь адаптировать код под свои нужды.

**Q: Что если у меня другой формат данных?**  
A: Отредактируй `01_Data_Preparation.ipynb` для преобразования твоего формата данных.

**Q: Как мне обновить результаты с новыми данными?**  
A: Просто заменить CSV файлы в `data/raw/` и переза
пустить все ноутбуки.

---

**Последнее обновление:** 29 ноября 2025